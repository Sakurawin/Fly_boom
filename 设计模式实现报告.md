# 飞机大战游戏设计模式实现报告

## 1. 项目概述

本项目在原有飞机大战游戏的基础上，实现了两个重要的设计模式：策略模式（Strategy Pattern）和数据访问对象模式（DAO Pattern）。这些模式的引入显著提升了代码的可维护性、可扩展性和结构清晰度。

## 2. 策略模式实现

### 2.1 模式简介

策略模式定义了一系列算法，将每个算法封装起来，并让它们可以相互替换。这让算法独立于使用它的客户端变化。

### 2.2 实现细节

#### 2.2.1 策略接口
```java
public interface ShootStrategy {
    List<BaseBullet> shoot(int x, int y, int direction, int power);
}
```

#### 2.2.2 具体策略实现

1. **直线射击策略（StraightShootStrategy）**
   - 实现单发子弹的直线射击
   - 适用于普通敌机和英雄飞机

2. **散射策略（ScatterShootStrategy）**
   - 实现多发子弹的散射效果
   - 增加射击覆盖范围，适用于精英敌机

3. **环形射击策略（CircularShootStrategy）**
   - 实现360度环形射击
   - 全方位覆盖，适用于Boss敌机

#### 2.2.3 策略应用

各类飞机在构造时设置相应的射击策略：
- HeroAircraft：StraightShootStrategy
- MobEnemy：StraightShootStrategy  
- EliteEnemy：ScatterShootStrategy
- BossEnemy：CircularShootStrategy

### 2.3 UML类图

![策略模式类图](uml/shootStrategy.puml)

### 2.4 模式优势

1. **开闭原则**：新增射击策略无需修改现有代码
2. **单一职责**：每个策略类只负责一种射击行为
3. **运行时切换**：可动态改变飞机的射击行为
4. **代码复用**：策略可在不同飞机间共享

## 3. 数据访问对象模式实现

### 3.1 模式简介

DAO模式提供了访问关系型数据库的抽象接口，通过这个接口，应用程序可以操作数据而不需要暴露数据库的实现细节。

### 3.2 实现细节

#### 3.2.1 数据传输对象
```java
public class GameScore {
    private int score;
    private LocalDateTime gameTime;
    private String playerName;
    private int duration;
    // 构造方法、getter和setter
}
```

#### 3.2.2 DAO接口
```java
public interface ScoreDao {
    boolean saveScore(GameScore gameScore);
    List<GameScore> getAllScores();
    List<GameScore> getTopScores(int limit);
    // 其他数据访问方法
}
```

#### 3.2.3 具体实现
```java
public class FileScoreDao implements ScoreDao {
    // CSV文件存储实现
    // 包含文件读写、数据解析等功能
}
```

#### 3.2.4 业务服务层
```java
public class ScoreService {
    private ScoreDao scoreDao;
    // 提供业务逻辑处理
    // 包含成绩保存、排行榜显示等功能
}
```

### 3.3 UML类图

![DAO模式类图](uml/scoreDao.puml)

### 3.4 模式优势

1. **数据访问抽象**：隐藏具体存储实现细节
2. **易于测试**：可方便地创建Mock对象进行单元测试
3. **存储方式切换**：可轻松从文件存储切换到数据库存储
4. **业务逻辑分离**：数据访问与业务逻辑清晰分离

## 4. 集成应用

### 4.1 游戏中的应用

在Game类中集成了ScoreService，实现了：
- 游戏结束时自动保存成绩
- 游戏历史记录查看
- 排行榜功能

### 4.2 道具系统扩展

新增了SuperBulletProp道具，获得后英雄飞机临时切换为环形射击策略，展示了策略模式的动态切换能力。

## 5. 技术特点

### 5.1 Java 8+ 特性应用
- Stream API用于数据筛选和排序
- LocalDateTime用于时间处理
- Lambda表达式简化代码

### 5.2 文件I/O处理
- CSV格式的数据持久化
- 异常处理和资源管理
- 文件自动创建机制

### 5.3 单例模式保持
- HeroAircraft继续使用单例模式
- 与策略模式完美结合

## 6. 测试验证

通过以下测试验证了实现的正确性：
1. 策略模式测试：验证不同射击策略的正确性
2. DAO模式测试：验证数据持久化和检索功能
3. 集成测试：验证游戏运行时的整体功能

## 7. 总结

本次重构成功引入了策略模式和DAO模式，显著提升了代码质量：

1. **可扩展性**：新增射击策略和存储方式变得容易
2. **可维护性**：代码结构清晰，职责分离明确
3. **可测试性**：各组件可独立测试
4. **代码复用**：策略和DAO组件可在其他项目中复用

这些改进为游戏的后续开发奠定了良好的架构基础，体现了良好的软件工程实践。